{
  "mapbox": {
    "enabled": true,
    "publicToken": "",
    "profile": "mapbox/driving",
    "geometries": "geojson",
    "overview": "full",
    "cacheInLocalStorage": true
  },
  "manualOverridesEnabled": true,
  "notes": "Add a Mapbox public access token here or store one in browser localStorage as journeylines.mapboxToken. Driving routes use Mapbox Directions when a token is available; boat/train use manual overrides for now."
}
